server:
  port: 8087

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/final_project
    username: root
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
